# 测试指南

## 如何测试 Hint 扩展

### 1. 加载扩展到浏览器

1. 运行构建命令：
```bash
pnpm build
```

2. 打开 Chrome 浏览器，访问 `chrome://extensions/`

3. 开启右上角的"开发者模式"

4. 点击"加载已解压的扩展程序"

5. 选择 `build/chrome-mv3-prod` 目录

### 2. 配置 API Key

1. 点击浏览器工具栏中的 Hint 扩展图标

2. 在弹出的设置面板中输入您的 API Key

3. （可选）配置自定义模型 URL 和模型名称

4. 点击"保存"按钮

### 3. 测试功能

#### 测试 1: 检查浮动工具栏显示

1. 访问任意支持的 AI 网站（例如：https://chatgpt.com）

2. 找到并点击页面上的可编辑文本区域（通常是输入框）

3. **预期结果**: 应该在输入框上方看到一个浮动的工具栏，包含一个闪电图标 ⚡

#### 测试 2: 测试提示词优化功能

1. 在输入框中输入一些文本，例如："帮我写一篇关于AI的文章"

2. 点击浮动工具栏上的闪电图标 ⚡

3. **预期结果**:
   - 按钮变成加载动画（旋转图标）
   - 开始显示优化后的文本（流式输出）
   - 按钮变成勾选图标 ✓

#### 测试 3: 测试文本替换功能

1. 优化完成后，点击勾选图标 ✓

2. **预期结果**:
   - 原始文本被替换为优化后的文本
   - 工具栏恢复到初始状态（闪电图标）

#### 测试 4: 测试取消功能

1. 在优化过程中，点击关闭按钮 ✕

2. **预期结果**:
   - 优化过程被中断
   - 工具栏恢复到初始状态

#### 测试 5: 测试错误处理

1. 在设置中输入一个无效的 API Key

2. 尝试优化文本

3. **预期结果**:
   - 显示错误消息（红色背景）
   - 可以通过关闭按钮清除错误

#### 测试 6: 测试暗色模式

1. 将系统主题切换到暗色模式

2. 刷新 AI 网站页面

3. **预期结果**:
   - 浮动工具栏应该自动适应暗色主题
   - 背景变为深色，文字变为浅色

### 4. 支持的网站列表

测试时请在以下网站上验证：

- ✅ https://chatgpt.com
- ✅ https://chat.openai.com
- ✅ https://claude.ai
- ✅ https://gemini.google.com
- ✅ https://yuanbao.tencent.com
- ✅ https://chat.deepseek.com
- ✅ https://grok.com

### 5. 常见问题排查

#### 问题：浮动工具栏没有显示

**可能原因**:
1. 扩展未正确加载
2. 页面上没有可编辑元素
3. 页面需要刷新

**解决方案**:
1. 检查 `chrome://extensions/` 确认扩展已启用
2. 刷新页面 (F5)
3. 打开浏览器控制台 (F12) 检查是否有错误
4. 确认网站 URL 匹配支持列表

#### 问题：优化功能不工作

**可能原因**:
1. API Key 未配置或无效
2. 网络连接问题
3. API 服务不可用

**解决方案**:
1. 重新检查 API Key 配置
2. 检查网络连接
3. 查看浏览器控制台的错误信息
4. 尝试使用不同的 API 端点

#### 问题：样式显示异常

**可能原因**:
1. CSS 文件未正确加载
2. 网站样式冲突

**解决方案**:
1. 重新构建扩展: `pnpm build`
2. 检查是否使用了 Shadow DOM 隔离
3. 查看控制台的样式相关错误

### 6. 调试技巧

#### 查看 Content Script 日志

1. 在 AI 网站页面上打开开发者工具 (F12)
2. 切换到 Console 标签
3. 查找扩展相关的日志信息

#### 查看 Background Script 日志

1. 访问 `chrome://extensions/`
2. 找到 Hint 扩展
3. 点击"service worker"链接
4. 在打开的开发者工具中查看日志

#### 查看存储的设置

在控制台中运行：
```javascript
chrome.storage.sync.get(null, (data) => console.log(data))
```

### 7. 性能测试

#### 检查内存使用

1. 打开任务管理器 (Shift + Esc)
2. 查找 Hint 扩展的内存使用
3. **预期**: 应该小于 50MB

#### 检查响应速度

1. 点击优化按钮后，记录响应时间
2. **预期**: 应该在 1-2 秒内开始流式输出（取决于网络和 API 速度）

### 8. 报告问题

如果发现任何问题，请提供以下信息：

1. 浏览器版本
2. 扩展版本
3. 出现问题的网站 URL
4. 控制台错误信息（截图或文本）
5. 重现步骤

在 GitHub Issues 中提交问题：[项目地址]

## 开发模式测试

如果您在开发模式下测试：

```bash
pnpm dev
```

开发模式的优势：
- 自动热重载
- 更详细的错误信息
- Source maps 支持

**注意**: 开发模式下的构建输出在 `build/chrome-mv3-dev/` 目录。
